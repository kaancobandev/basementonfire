// LazyMotion için framer-motion animasyon özellikleri — ayrı (async) chunk.
// domAnimation: animate/exit/variants + tap-hover-focus jestleri (drag/layout yok;
// HomeFeed bunları kullanmıyor). Bkz. HomeFeed'deki LazyMotion sarmalayıcısı.
export { domAnimation as default } from 'framer-motion';
