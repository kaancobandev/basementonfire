# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...ama ŞABLON commit'lenir, yoksa repoyu klonlayan hangi degiskenlerin
# gerektigini hic goremez. Icinde YALNIZCA yer tutucu olmali; gercek deger
# .env.local'e yazilir ve o yukaridaki kuralla yok sayilmaya devam eder.
!.env.local.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# supabase CLI'nin yerel durumu — `supabase link` sonrası kendiliğinden oluşur.
# Göçler bu klasörde DEĞİL, sql/ altında elle yürütülüyor; burada paylaşılmaya
# değer hiçbir şey yok. Ayrıca .temp/ arka uç sürümlerini (Postgres, GoTrue,
# Storage) tam tam yazıyor → depoya koymak karşılığında hiçbir fayda vermeden
# sürüme özel açık aramayı kolaylaştırır. İleride migration workflow'una
# geçilirse supabase/migrations ve supabase/config.toml AYRICA izin verilmeli.
/supabase/.temp/

# Göç dökümleri — kullanıcı e-postaları ve tüm veri burada, ASLA commitlenmez
/goc-dokum/

# Denetim/olcum betiklerinin biraktigi gecici indirmeler
*.bin
_t.mjs
_tt/
